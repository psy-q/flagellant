<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" width="<%= @base_layer.width %>" height="<%= @base_layer.height %>">

<g id="base_layer">
  <%= render :partial => "flags/base_layers/#{@base_layer.template_name}.svg.erb" %>
</g>

<% @layers.each do |layer| %>
  <%= render :partial => "flags/layers/#{layer.template_name}.svg.erb" %>
<% end %>

</svg>
